<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.market4me">

    <!-- Le indicamos a google play que esta app usa la camara pero no es indispensable para la app -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <!-- Le indicamos a google play que esta app escribe y lee en memoria (para guardar las fotos)  -->

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivityTest" />
        <activity android:name=".RecipeListActivity" />
        <activity android:name=".DisplayRecipeActivity" />
        <activity android:name=".HomeDashBoardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewRecipeActivity"
            android:parentActivityName=".RecipeListActivity" />

        <!-- IMPORTANTE -->
        <!-- Con el atributo anterior, ponemos la flechita en la barra superior, para poder volver a la actividad anterior -->


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.market4me.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>